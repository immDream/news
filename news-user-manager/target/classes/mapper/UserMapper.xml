<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.immdream.usermanager.mapper.UserMapper">

    <!-- 开启二级缓存 -->
    <!--<cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>-->

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.immdream.model.domain.user.User">
        <result column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="nickname" property="nickname" />
        <result column="email" property="email" />
        <result column="telephone" property="telephone" />
        <result column="image" property="image" />
        <result column="describe" property="describe" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        `id`,
        `username`, `password`, `nickname`, `email`, `telephone`, `image`, `describe`, `create_time`, `update_time`
    </sql>

    <select id="selectUserListByCondition" parameterType="com.immdream.model.domain.user.User" resultMap="BaseResultMap">
        select <include refid="Base_Column_List" /> from USER
        <where>
            <if test="username != null">
                and username = #{username}
            </if>
            <if test="telephone != null">
                union select <include refid="Base_Column_List" /> from USER
                where telephone = #{telephone}
            </if>
            <if test="email != null">
                union select <include refid="Base_Column_List" /> from USER
                where email = #{email}
            </if>
        </where>
    </select>

</mapper>
